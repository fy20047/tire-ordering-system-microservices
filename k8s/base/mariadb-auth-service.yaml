apiVersion: v1
kind: Service
metadata:
  name: mariadb-auth
  labels:
    app: mariadb-auth
spec:
  clusterIP: None
  ports:
    - name: mariadb
      port: 3306
      targetPort: 3306
  selector:
    app: mariadb-auth
