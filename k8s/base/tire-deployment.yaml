# 這份檔案用途：
# 定義 Tire Service 在 Kubernetes 的 Deployment，承接公開與後台輪胎 API。
apiVersion: apps/v1
kind: Deployment
metadata:
  # 這段用途：定義 Deployment 名稱與標籤，供 Service/監控識別。
  name: tire-service
  labels:
    app: tire-service
spec:
  # 這段用途：先採單副本，降低 Phase 3 切流風險。
  replicas: 1
  progressDeadlineSeconds: 600
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tire-service
  template:
    metadata:
      labels:
        app: tire-service
    spec:
      containers:
        - name: tire-service
          image: ghcr.io/fy20047/tire-ordering-system-microservices/tire-service:latest
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            # 這段用途：載入 app-secret 內的 JWT key（與其他服務一致管理）。
            - secretRef:
                name: app-secret
          env:
            # 這段用途：從 db-secret 讀取 DB 帳號密碼，避免明文硬編碼。
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: MARIADB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: MARIADB_PASSWORD
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 384Mi
          readinessProbe:
            # 這段用途：用 actuator health 判斷是否可接流量。
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            # 這段用途：用 TCP 探針確認程序存活，避免假死不重啟。
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
