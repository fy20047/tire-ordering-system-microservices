apiVersion: v1
kind: Service
metadata:
  name: mariadb-tire
  labels:
    app: mariadb-tire
spec:
  clusterIP: None
  ports:
    - name: mariadb
      port: 3306
      targetPort: 3306
  selector:
    app: mariadb-tire
