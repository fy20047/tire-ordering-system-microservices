apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: backend
spec:
  # 最小可用數量為 2
  # 告訴 K8s 管理員或自動化工具必須保證隨時至少有 2 個 backend Pod 活著
  # 如果有 3 個 Pod，管理員一次只能殺 1 個 (3-1=2)，等新的補上來後才能殺下一個
  minAvailable: 2
  selector:
    matchLabels:
      app: backend # 保護標籤為 app: backend 的 Pod
